import { IHandler, IWebSocketHandlers } from "../bao";
import { TMethods } from "../router";
import { Node } from "./tree";
export declare class BaoRouter {
    trees: {
        [key: string]: Node;
    };
    opts: {
        [key: string]: any;
    };
    constructor(opts?: {});
    any(path: string, handler: IHandler): void;
    ws(path: string, handlers: IWebSocketHandlers): this;
    on(method: TMethods, path: string, handler: IHandler): this;
    find(method: string, path: string): IRouterResponse;
}
export interface IRouterResponse {
    handler: IHandler | IWebSocketHandlers | null;
    params: {
        [key: string]: string;
    };
}
