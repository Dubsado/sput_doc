import { IHandler, IWebSocketHandlers } from "../bao";
import { IRouterResponse } from "./router";
export declare class Node {
    path: string;
    wildChild: boolean;
    type: number;
    indices: string;
    children: Node[];
    handler: IHandler | IWebSocketHandlers | null;
    priority: number;
    constructor(path?: string, wildChild?: boolean, type?: number, indices?: string, children?: any[], handler?: any, priority?: number);
    /**
     *
     * @param {number} pos
     */
    addPriority(pos: any): any;
    /**
     * Adds a node with the given handler to the path
     */
    addRoute(path: string, handler: IHandler | IWebSocketHandlers): void;
    insertChild(path: string, fullPath: string, handler: IHandler | IWebSocketHandlers): void;
    search(path: string): IRouterResponse;
}
